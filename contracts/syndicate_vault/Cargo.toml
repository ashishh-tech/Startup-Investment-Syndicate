[package]
name = "syndicate_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
waterfall_distributor = { path = "../waterfall_distributor" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
